import LearnLean
-- import «LearnLean»

def main : IO Unit :=
  IO.println s!"Hello, {hello}!"

-- def pred (x : α) := ∀ P : α → Prop, P x

-- #check (@pred : α → Prop)

-- #check pred x

-- #check Quot
